#include "AI0.h"
#include <cstdlib>
#include <iostream>
#include <algorithm>
using namespace std;

pair<int, int> AI0_getPoint(const int M, const int N, const int* top, int** _board, const int lastX, const int lastY, const int noX, const int noY) {
	return make_pair(0, 0);
}
